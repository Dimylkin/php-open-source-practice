## 🚀 Как запустить проект

### Требования

- PHP >= 8.0
- MySQL >= 5.7
- Composer
- Git

### Установка

1. Клонируйте репозиторий:

```bash
git clone https://github.com/Dimylkin/php-open-source-practice.git
cd php-open-source-practice
```

2. Установите зависимости (если есть):

```bash
composer install
```

3. Настройте базу данных (для практических работ с БД):

```bash
mysql -u root -p < database/schema.sql
```

4. Запустите встроенный сервер:

```bash
php -S localhost:8000
```

5. Откройте в браузере:
```
http://localhost:8000
```

## 📝 Требования к коду

### Стандарт кодирования: PSR-12

Весь код должен соответствовать стандарту  **PSR-12**:

- Строгая типизация: `declare(strict_types=1);`
- 4 пробела для отступов (не табы)
- Открывающая фигурная скобка на новой строке для функций и классов
- PHPDoc для всех публичных методов
- Максимальная длина строки: 120 символов

### Безопасность

- ✅ Используйте подготовленные запросы (PDO)
- ✅ Экранируйте вывод: `htmlspecialchars($data, ENT_QUOTES, 'UTF-8')`
- ✅ Валидируйте пользовательский ввод
- ✅ Защита от XXE: используйте флаги `LIBXML_NONET`

## 🔀 Как отправить Pull Request

### 1. Форкните репозиторий

Нажмите кнопку  **Fork** в правом верхнем углу страницы GitHub.

### 2. Клонируйте свой форк

```bash
git clone https://github.com/ВАШ_USERNAME/php-open-source-practice.git
cd php-open-source-practice
```

### 3. Создайте ветку для изменений

```bash
git checkout -b fix/issue-123
# или
git checkout -b feature/add-validation
```

Название ветки:
- `fix/` — для исправлений
- `feature/` — для новых функций
- `docs/` — для изменений документации

### 4. Внесите изменения

Редактируйте код, соблюдая стандарты PSR-12.

### 5. Коммит изменений

```bash
git add .
git commit -m "fix: исправление XSS в форме регистрации"
```

### 6. Отправьте изменения

```bash
git push origin fix/issue-123
```

### 7. Создайте Pull Request

1. Откройте ваш форк на GitHub
2. Нажмите  **"Compare & pull request"**
3. Заполните описание:
   - Что изменено
   - Почему это нужно
   - Ссылка на issue (если есть)
4. Нажмите  **"Create pull request"**

### Требования к PR

- ✅ Код соответствует PSR-12
- ✅ Все функции документированы
- ✅ Нет ошибок и warnings
- ✅ Описание понятно объясняет изменения

## 📌 Как писать коммиты

Используйте  **Conventional Commits**:

### Формат

```
<тип>: <краткое описание>

<подробное описание (опционально)>

<футер (опционально)>
```

### Типы коммитов

-  `feat:` — новая функциональность
-  `fix:` — исправление бага
-  `docs:` — изменения в документации
-  `style:` — форматирование кода (без изменения логики)
-  `refactor:` — рефакторинг кода
-  `test:` — добавление или изменение тестов
-  `chore:` — обновление зависимостей, конфигурации